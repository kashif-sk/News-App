import {StyleSheet} from 'react-native';
import {ArticleListStyle} from './types';

const styles = StyleSheet.create<ArticleListStyle>({
  flatlistContentContainer: {
    alignItems: 'center',
  },
});

export default styles;
